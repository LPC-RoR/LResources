<div>
		<%= render partial: '0p/general/gly_title_size', locals: {gly: 'images', title: "Imagenes escanedas", size: 6 } %>
		<table class="table <%= table_types_base[:borderless] %> table-sm">
		  <tbody>

		  	<!-- Archivos-->
		  	<% unless @coleccion['app_imagenes'].blank? %>
			  	<% @coleccion['app_imagenes'].each do |objeto| %>
				  	<%= render partial: "/repositorios/app_imagenes/partials/modelo", locals: {objeto: objeto} %>
		  		<% end  %>
		  	<% end %>

		  </tbody>
		</table>

		<%= render partial: '0p/tabla/btn_nuevo_o', locals: {o: @objeto, c: 'app_imagenes'} %>
</div>