<%= form_with(model: app_escaneo) do |form| %>
  <% if app_escaneo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(app_escaneo.errors.count, "error") %> prohibited this app_escaneo from being saved:</h2>

      <ul>
        <% app_escaneo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ownr_class, style: "display: block" %>
    <%= form.text_field :ownr_class %>
  </div>

  <div>
    <%= form.label :ownr_id, style: "display: block" %>
    <%= form.number_field :ownr_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
