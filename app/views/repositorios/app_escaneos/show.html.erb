<%= render partial: '0p/general/gly_title_size', locals: {gly: controller_icon['app_escaneos'], title: "Escaneo del día : #{dma(@objeto.created_at.to_date)}", size: 4 } %>

<%= render partial: '/repositorios/partials/link_owner' %>

<div class='row'>
  <div class="col-md-6">
    <%= render partial: 'repositorios/app_escaneos/partials/directorio' %>
  </div>
  <div class="col-md-6">
    
  </div>
</div>

<hr>

<!-- Despliegue de imágenes-->
<% unless @coleccion['app_imagenes'].blank? %>
  <% @coleccion['app_imagenes'].each do |objeto| %>
    <%= image_tag(objeto.app_imagen.url) if objeto.app_imagen? %>
    <hr>
  <% end  %>
<% end %>
