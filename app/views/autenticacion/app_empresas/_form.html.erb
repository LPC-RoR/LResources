<%= form_with(model: app_empresa) do |form| %>
  <% if app_empresa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(app_empresa.errors.count, "error") %> prohibited this app_empresa from being saved:</h2>

      <ul>
        <% app_empresa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :app_empresa, style: "display: block" %>
    <%= form.text_field :app_empresa %>
  </div>

  <div>
    <%= form.label :rut}, style: "display: block" %>
    <%= form.text_field :rut} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
